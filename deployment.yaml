apiVersion: apps/v1
kind: Deployment
metadata:
  name: expressapi6
  labels:
    app: expressapi6
spec:
  replicas: 1
  selector:
    matchLabels:
      app: expressapi6
  template:
    metadata:
      labels:
        app: expressapi6
    spec:
      containers:
        - name: expressapi6
          image: ${REGISTRY}/${REPOSITORY}:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: expressapi-service6
spec:
  selector:
    app: expressapi6
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: LoadBalancer
