apiVersion: apps/v1
kind: Deployment
metadata:
  name: expressapi3
  labels:
    app: expressapi3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: expressapi3
  template:
    metadata:
      labels:
        app: expressapi3
    spec:
      containers:
        - name: expressapi3
          image: ${REGISTRY}/${REPOSITORY}:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: expressapi-service3
spec:
  selector:
    app: expressapi3
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: LoadBalancer
