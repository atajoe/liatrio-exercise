apiVersion: apps/v1
kind: Deployment
metadata:
  name: expressapi4
  labels:
    app: expressapi4
spec:
  replicas: 1
  selector:
    matchLabels:
      app: expressapi4
  template:
    metadata:
      labels:
        app: expressapi4
    spec:
      containers:
        - name: expressapi4
          image: ${REGISTRY}/${REPOSITORY}:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: expressapi-service4
spec:
  selector:
    app: expressapi4
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: LoadBalancer
